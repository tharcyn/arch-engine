# Release Certification Record

**Repository:** @arch-engine/core
**Version:** v0.1.0-preview-patch1
**Commit:** 99838fb
**Certification Type:** Deterministic Replay Integrity Verification
**Certification Scope:**

- snapshot replay invariance
- ordering determinism
- hash lineage integrity
- schema authority stability
- export surface freeze
- workspace isolation guarantees

---

## 1. SNAPSHOT REPLAY STATUS: **VERIFIED** ✅

- All 17 snapshot files pass without update
- No snapshot ordering drift
- No snapshot serialization drift
- `Object.keys().sort()` on `Object.create(null)` produces identical enumeration as `{}` — verified via runtime proof
- `JSON.stringify()` round-trip for null-prototype objects is byte-identical to plain objects
- Policy graph materialization unchanged

---

## 2. CROSS-ENVIRONMENT ORDERING STATUS: **VERIFIED** ✅

- **0 executable `localeCompare` calls** remain in `packages/core/src/`
- 20 `localeCompare` calls replaced with `binaryStringCompare(a, b)` across 3 files
- `binaryStringCompare` uses `a < b ? -1 : a > b ? 1 : 0` — JavaScript spec-guaranteed UTF-16 code unit ordering
- Verified provider ordering stability
- Verified namespace ordering stability
- Verified overlay merge tie-break ordering stability
- Verified rejected-provider sort ordering stability
- `semverSelectionContract.ts` line 12 mentions `localeCompare` in a **documentation comment only** — its runtime code at line 33 already uses binary comparison `a < b ? -1 : a > b ? 1 : 0`

---

## 3. POLICY COMPOSITION GRAPH STATUS: **VERIFIED** ✅

- `finalDomains` uses `Object.create(null)` — enumeration is identical to `{}`
- `Object.keys()`, `Object.entries()`, `JSON.stringify()` all produce identical results — verified via Node runtime proof
- Domain merge precedence: unchanged (depth-ordered iteration, spread-merge semantics)
- Rule resolution ordering: unchanged (Map insertion order preserved, content-addressed keying unchanged)
- Hash input path (`JSON.parse(JSON.stringify(preHash))` → `Object.keys(obj).sort()`) passes through JSON serialization which normalizes null-prototype objects to plain objects
- Composition graph traversal order: unchanged

---

## 4. HASH LINEAGE STATUS: **VERIFIED** ✅

- `canonicalPolicyHash` input ordering: unchanged (uses `Object.keys().sort()` after JSON round-trip)
- `effectiveHash` in compositionResolver: unchanged (same JSON round-trip → sort → stringify → SHA-256 path)
- `stableCanonicalStringify` key ordering: uses `Buffer.from(a).compare(Buffer.from(b))` — verified alignment with `binaryStringCompare` via runtime proof over 11 string pairs: **all match**
- No timestamp influence in any hash path
- No filesystem path influence in any hash path
- No locale influence (localeCompare eliminated)
- No environment-variable influence in any hash path

---

## 5. SEMVER CONTRACT STATUS: **VERIFIED** ✅

- Valid versions accepted: `1.2.3`, `0.0.0`, `10.20.30` — pass regex `/^\d+\.\d+\.\d+$/`
- Pre-release versions accepted: `1.0.0-beta.1`, `2.1.3-rc.2` — suffix stripped before validation
- Invalid versions rejected: `abc`, `1.2`, `1`, `""`, `1.2.3.4`, `v1.2.3` — verified via runtime proof
- No valid historical manifest format becomes invalid (all formats in manifest JSON are `MAJOR.MINOR.PATCH`)
- `semverGte` behavior unchanged for valid inputs — same numerical comparison logic
- Capability negotiation engine's `parseSemver` (separate copy, line 80-84) unchanged — uses regex match, not `split('.').map(Number)`

---

## 6. YAML INGESTION GUARD STATUS: **VERIFIED** ✅

- Guard executes **pre-parse only** — `statSync` before `readFileSync`
- Guard does not affect serialization inputs — operates before YAML parse
- Guard does not affect hash lineage — operates before `canonicalPolicyHash` call
- Error message is deterministic: includes byte count and path
- Error does not leak filesystem metadata beyond the policy path (which is already known to the caller)
- `MAX_POLICY_FILE_BYTES` is a module-scoped constant, not configurable

---

## 7. EXPORT SURFACE STATUS: **FROZEN** ✅

- Root exports (`index.d.ts`): same 2 export lines, same symbols
- Subpath exports (`analysis.d.ts`, `parsers.d.ts`): unchanged
- `binaryStringCompare` appears **0 times** in any `.d.ts` file — internal only
- No new public types, no new public error classes
- No CLI surface bleed into core
- No accidental symbol widening

---

## 8. SCHEMA AUTHORITY STATUS: **VERIFIED** ✅

All 17 schema `$id` fields resolve under canonical authority:

```
https://schemas.arch-engine.dev/
```

Zero occurrences of non-canonical `https://arch-engine.dev/schemas/` anywhere in tracked files.

---

## 9. DIST PARITY STATUS: **VERIFIED** ✅

- Fresh build: ESM + CJS + DTS generated successfully
- `.d.ts` signatures: 3 entry points unchanged
- Runtime structure: 3 entry points unchanged
- Internal utilities remain internal — `binaryStringCompare` NOT in dist type exports
- `vendoor` appears 0 times in dist `.d.ts` files

---

## 10. CLI ISOLATION STATUS: **SAFE** ✅

- `@arch-engine/adapter-monorepo` appears **0 times** in `packages/core/src/`
- `@arch-engine/adapter-monorepo` appears **0 times** in `packages/core/dist/`
- `@arch-engine/cli` appears **0 times** in `packages/core/dist/`
- CLI remains workspace-only, non-publishable companion with no coupling to core

---

## 11. CANONICAL STRINGIFICATION STATUS: **VERIFIED** ✅

- `stableCanonicalStringify.ts` sortKeys uses `Buffer.from(a).compare(Buffer.from(b))` for key ordering
- `binaryStringCompare` uses `a < b ? -1 : a > b ? 1 : 0` (UTF-16 code unit ordering)
- Runtime proof over 11 representative string pairs: **all signs match**
- For ASCII-range strings (all identifiers in this system), UTF-16 code unit ordering and UTF-8 byte ordering are identical
- Canonical ordering pipeline is internally consistent

---

## FINAL CERTIFICATION VERDICT

### **REPLAY SAFE — TAG APPROVED** ✅

```
Tag:    v0.1.0-preview-patch1
Commit: 99838fb
Tests:  915/915 pass
Build:  clean
Export: frozen
Schema: verified
Hash:   stable
```

---

## Certification Intent

This certification record anchors deterministic replay guarantees for
@arch-engine/core v0.1.0-preview-patch1 and establishes a verification
baseline for subsequent preview and stable releases.

Future certification records should extend this chain to preserve
cross-version determinism lineage.
