# Public Surface Freeze Certificate — @arch-engine/core

## 1. Purpose of the Public Surface Freeze Certificate

This document formally certifies the execution-surface stability of `@arch-engine/core`.

It declares that deterministic identity participation is mathematically frozen, confirms that federation neutrality guarantees are structurally stabilized, and defines execution extraction readiness for release-candidate (`v1.0.0-rc.1`) tagging.

## 2. Execution Surface Freeze Certification

The core topological evaluation boundary is formally frozen:
- **`EngineRunner` orchestration surface**: The initialization and execution pipeline APIs are stable.
- **Pipeline transition phases**: Discovery, resolution, and evaluation steps operate stably.
- **Execution topology construction**: Dependency mapping builds stably.
- **`closureGraphHash` generation participation**: Inputs driving hash formation are explicitly mathematically locked.

## 3. Semantic Identity Surface Freeze Certification

Aligning strictly with `identity-surface-contract.md`:
- **Identity-participating inputs**: The subset of explicit topological nodes mapped into the graph is mathematically finalized.
- **Identity-neutral inputs**: Distribution, observation, and transport-level environmental states are explicitly finalized as excluded mappings.
- **Transport-layer neutrality**: Assurances decoupling routing from semantics are stabilized.
- **Execution envelope participation**: The entire structural payload boundary is formally locked.

## 4. Loader Pipeline Freeze Certification

The structural mechanics governing discovery are formally frozen:
- **`policy://` URI resolution semantics**: Stable string interpolation paths mapping efficiently to distribution topologies.
- **Manifest resolution pipeline**: The JSON definition mapping logic is completely stable.
- **Registry provenance routing**: Deterministic URI resolution logic is completely stable.
- **Mirror fallback neutrality**: Resiliency execution fetching logic remains completely stable.
- **Dependency closure resolution**: The recursive acyclic topological ordering operations are firmly stable.

## 5. Overlay Merge Algebra Freeze Certification

Aligning explicitly with `execution-model.md`:
- **Overlay precedence ordering**: The positional evaluation mapping rules remain mathematically stable.
- **Suppression participation semantics**: Logic nullifying baseline constraints evaluates stably.
- **Additive structural expansion semantics**: Logic appending new boundary shapes evaluates stably.
- **Merge determinism guarantees**: The algebra resolves strictly identically and deterministically every time.

## 6. Authority-Tier Eligibility Freeze Certification

The boundaries restricting logic execution capabilities are formally frozen:
- **`LOCAL_POLICY` eligibility semantics**: Untrusted origin boundaries operate cleanly safely.
- **`SIGNED_EXTERNAL_PACK` eligibility semantics**: Cryptographic authorization pathways evaluate securely perfectly stably.
- **`TRUSTED_POLICY_PACK` eligibility semantics**: High-assurance topologies map reliably dependably.
- **Eligibility filtering participation in identity**: Preserved accurately cleanly.

## 7. Capability Adapter Observation Boundary Freeze Certification

Aligning strictly with `capability-model.md`:
- **Capability adapters remain observation-layer components**: Stable observation trace isolation.
- **Capability adapters MUST NOT mutate deterministic execution identity**: Tools only read or discover safely.
- **Adapter topology injection boundaries explicitly preserved**: Safely locked interfaces.

## 8. Snapshot Replay Determinism Freeze Certification

Aligning with `determinism-contract.md`:
- **Snapshot replay identity equivalence guarantees stable**.
- **Serialization envelope neutrality stabilized**.
- **Execution host independence preserved**.

## 9. Registry Federation Neutrality Freeze Certification

Aligning with `registry-federation-contract.md`:
- **Multi-registry participation identity neutrality preserved**.
- **Mirror fallback neutrality preserved**.
- **Transport routing neutrality preserved**.

## 10. Policy Pack Participation Surface Freeze Certification

Aligning with `policy-pack-contract.md` and `reference-policy-pack-authoring.md`:
- **Policy pack structural participation surface stable**.
- **Overlay compatibility expectations stable**.
- **Provenance lineage participation stable**.

## 11. Version Compatibility Window Freeze Certification

Aligning with `versioning-strategy.md`:
- **Identity-preserving schema windows stabilized**.
- **Forward-compatible topology participation expectations preserved**.
- **Identity-breaking transitions explicitly governed**.

## 12. Public Export Surface Freeze Certification

- **Barrel exports stable**: Root API points cleanly defined safely precisely.
- **Contract interfaces stable**: Native type aliases are complete reliably successfully.
- **Adapter participation surfaces stable**.
- **Schema validation entrypoints stable**.

## 13. Release-Candidate Extraction Readiness Declaration

I formally and definitively declare:
- `@arch-engine/core` ready for v1.0.0-rc.1 tagging
- deterministic execution surface stable
- semantic identity participation surface sealed
- loader pipeline semantics frozen
- federation neutrality guarantees stabilized
- snapshot replay determinism preserved
- ecosystem participation safe
