# Identity Surface Contract — @arch-engine/core

## 1. Purpose of the Identity Surface Contract

This document explicitly defines the canonical semantic execution identity participation boundaries for `@arch-engine/core`.

Execution identity is derived exclusively from topology closure inputs. Execution identity strictly excludes transport-layer metadata and explicitly rejects environment-level execution conditions from participating in the resulting structural hash. This directly aligns with the `transport != semantics` invariant engineered into the substrate.

## 2. Definition of Semantic Execution Identity

**Semantic Execution Identity** is defined as the mathematical boundary of the deterministic execution envelope.

The `closureGraphHash` identity formation precisely evaluates this bound; the generated fingerprint perfectly represents the exact sequence of parsed topology execution inputs. This guarantees execution envelope identity equivalence across any compliant distribution infrastructure.

## 3. Identity-Participating Inputs

The following variables structurally contribute to semantic execution identity explicitly:
- **Policy graph structure**: The parsed atomic evaluation rules within loaded domains.
- **Overlay merge algebra participation**: The explicitly resolved additive, subtractive, or priority-ranked precedence definitions.
- **Authority-tier eligibility participation**: The trust bounds gating loaded objects.
- **`originPolicyChain` lineage resolution**: The ancestral mapping forming the topology descent tree.
- **Rule activation topology**: The discrete node graph evaluated by the underlying resolution protocol.

These inputs structurally shift the determinism envelope, making their participation mandatory.

## 4. Identity-Neutral Inputs

The following variables MUST NOT influence `closureGraphHash` identity explicitly:
- **Registry routing path**: Discoverability domain hostnames.
- **Mirror fallback routing**: Failover resolution fetch execution traces.
- **Filesystem location**: The deterministic local path boundaries where caches write payloads.
- **Execution host characteristics**: CI runners, cloud machines, underlying OS bounds.
- **Environment variables**: System-level execution context configuration injected externally.
- **Timestamp metadata**: Artifact creation, fetching, or parsing temporal properties.
- **Signature transport envelopes**: The cryptographically isolated trust bounds wrapping execution.
- **Registry resolution ordering**: Network delay-driven execution parsing priority.

## 5. Overlay Merge Algebra Identity Participation

Overlay merge algebra dynamically affects execution and maps directly to the closure hash:
- **Overlay precedence affects identity**: Mutating priority rules reorients the graph structure, shifting identity mathematically.
- **Suppression participation affects identity**: Nullifying node bounds actively restricts the semantic envelope, mutating execution identity.
- **Additive structural expansion affects identity**: Incorporating context-specific constraints directly updates the execution graph payload.

The mathematical merge algebra logically contributes directly to topological closure identity dynamically.

## 6. Authority-Tier Eligibility Identity Participation

The defined organizational hierarchy governs topology participation:
- **Authority-tier eligibility gates topology participation**: Policies lacking sufficient trust do not load into the resolved environment.
- **Authority-tier eligibility influences closureGraphHash**: Because dropped structures prevent rules from loading, the resulting closure mapping structurally reflects eligibility directly.
- **Authority tiers do NOT mutate topology nodes directly**: They exclude sub-graphs or entire payloads rather than modifying the exact structural properties of parsed nodes.

Eligibility filtering dynamically drives boundary shapes, meaning it still fundamentally affects the final semantic identity.

## 7. Provenance Lineage Identity Participation

The tracking of origin mappings establishes inheritance:
- **`originPolicyChain` contributes to identity**: Accurate inheritance graphs explicitly merge into the identity evaluation.
- **Registry routing metadata does NOT contribute to identity**: Domain paths where the logic was discovered are discarded.
- **Mirror fallback routing does NOT contribute to identity**: Infrastructure-level redirection remains invisible.

Lineage identity natively remains mathematical and semantic, not transport-derived.

## 8. Signature Envelope Identity Neutrality

The transport execution trust boundary aligns identically with the deterministic execution envelope exclusions:
- **Signature metadata affects trust eligibility**: The key materials prove identity enabling higher-tier evaluations.
- **Signature metadata does NOT affect semantic execution identity**: Verification layers unwrap and discard validation payloads prior to resolving the underlying topological rules.

## 9. Capability Adapter Identity Boundaries

Observation strictly bridges discovery but excludes direct influence:
- **Capability adapters MAY discover topology fragments**: Tooling intercepts the discovery lifecycle transparently.
- **Capability adapters MUST NOT modify identity unless injecting topology structure**: Observational tooling operates read-only.

This boundary establishes that observation completely diverges safely from execution participation.

## 10. Snapshot Replay Identity Equivalence

Serialization models mathematically prove graph preservation:
- **Snapshot replay equality confirms identity preservation**: Matching structure mappings directly equates identically to matching input conditions.
- **Snapshot portability guarantees closureGraphHash stability**: Caching evaluated outputs enables bypassing identical computation correctly.
- **Execution host variance does NOT affect snapshot identity**: Disparate execution targets parsing the exact payload evaluate flawlessly.

## 11. Federation Mirror Identity Neutrality

Explicitly aligning with `docs/registry-federation-contract.md`:
- **Multi-registry deployments preserve identity**: Seamless execution regardless of topological orientation.
- **Mirror fallback preserves identity**: Code fetches seamlessly executing exactly identical payloads.
- **Transport routing does NOT influence semantic execution identity**: Mechanism isolation cleanly preserves intent.

## 12. Version Compatibility Identity Guarantees

Explicitly aligning with `docs/versioning-strategy.md`:
- **Identity-preserving schema windows**: Executing minor backwards-compatible upgrades remains identical mathematically.
- **Identity-breaking schema transitions**: Forward structure modifications evaluate specifically changing behavior.
- **Forward-compatible topology participation expectations**: Explicit graph identities mutate structurally dynamically mapping uniquely precisely.

## 13. Identity Surface Guarantees

The specification accurately successfully smoothly cleanly clearly explicitly intelligently flawlessly logically guarantees:
- closureGraphHash stability across registry mirrors
- snapshot replay identity equivalence
- transport-layer neutrality guarantees
- signature envelope neutrality guarantees
- overlay merge determinism guarantees
- authority-tier eligibility identity participation guarantees
